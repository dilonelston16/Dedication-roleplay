<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin - Users - Dedication Roleplay</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="site-header">
    <div class="logo">
      <span class="logo-main">Dedication</span>
      <span class="logo-sub">Roleplay</span>
    </div>
    <nav>
      <a href="/">Home</a>
      <a href="/staff">Staff</a>
      <a href="/apply">Apply</a>
      <a href="<%= discordInvite %>" target="_blank" class="btn-discord">Join Discord</a>

      <% if (user) { %>
        <a href="/dashboard">Dashboard</a>
        <% if (user.role === 'admin' || user.role === 'owner') { %>
          <a href="/admin/users">Admin</a>
        <% } %>
        <a href="/applications">Applications</a>
        <span class="user-tag"><%= user.discord_username %> (<%= user.role %>)</span>
        <a href="/logout" class="btn-secondary">Logout</a>
      <% } else { %>
        <a href="/login" class="btn-primary">Login with Discord</a>
      <% } %>
    </nav>
  </header>

  <main class="container">
    <h1>Admin · User Roles</h1>
    <p>Change roles to control who can access staff features.</p>

    <table class="table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Discord</th>
          <th>Current Role</th>
          <th>Change Role</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(function(u) { %>
          <tr>
            <td><%= u.id %></td>
            <td><%= u.discord_username %></td>
            <td><%= u.role %></td>
            <td>
              <form method="POST" action="/admin/users/<%= u.id %>/role">
                <select name="role">
                  <option value="owner" <%= u.role === 'owner' ? 'selected' : '' %>>owner</option>
                  <option value="member" <%= u.role === 'member' ? 'selected' : '' %>>member</option>
                  <option value="staff" <%= u.role === 'staff' ? 'selected' : '' %>>staff</option>
                  <option value="applications" <%= u.role === 'applications' ? 'selected' : '' %>>applications</option>
                  <option value="admin" <%= u.role === 'admin' ? 'selected' : '' %>>admin</option>
                </select>
                <button type="submit" class="btn-small">Save</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </main>

  <footer class="site-footer">
    <p>© <%= new Date().getFullYear() %> Dedication Roleplay · GTA V Roleplay Community</p>
  </footer>
</body>
</html>
